int n, temp;
     cin >> n;
     vector <int> a(n);
     for (int i = 0; i < n; i++) {
         cin >> a[i];}
     temp = a[n];
     for (int i = n; i > 0; i--){
         a[i] = a[i - 1];}
     a[0] = temp;
     for (int i = 0; i < n; i++) {
         cout << a[i] << ' ';}
    return 0;
}