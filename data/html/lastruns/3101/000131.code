#include <stdio.h>
#include <math.h>
int main(void){
    int n;
    int a = 0;
    scanf("%d", &n);
    while (n != 1){
        n = 1 + n / 2;
        a += 1;
    }
    printf("%d", a);
    return 0;
}
