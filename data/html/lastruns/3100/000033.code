from math import sqrt
number = int(input())
ans = []
for i in range(2, int(sqrt(number)) + 1):
    if number % i == 0:
        ans.append(i)
        ans.append(number // i)
print(*ans)        
        
