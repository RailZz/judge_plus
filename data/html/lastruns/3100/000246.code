#include <iostream>

using namespace std;

int main()

{
    long long int n;
    cin >> n;
    for (int i = 2; i*i < n; i++)
    {
        if ((n%i) == 0)
        {
            cout << "composite";
            return 0;
        }
    }
    cout << "prime";
    return 0;
}