#include <bits/stdc++.h>
using namespace std;

int gcd(int a, int b) {
    if (b == 0) return a;
    return gcd(b, a % b);
}
	
int main() {
    int n, megagcd = 0;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i<n; ++i) cin >> a[i];
    megagcd = gcd (a[0], a[1]);
    for (int i = 2; i<n; ++i) megagcd = gcd(megagcd, a[i]);

    cout << megagcd;

    return 0;
}