#include <bits/stdc++.h>
using namespace std;

int gcd(long long a, long long b) {
    while (b > 0) {
        a %= b;
        swap(a, b);
    }
    return a;
}

int main() {
    int n, k;
    cin >> n >> k;
    int nok = a * b / gcd(a, b);
    cout << nok;

    return 0;
}