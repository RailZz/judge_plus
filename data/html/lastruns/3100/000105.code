#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> vec;
    while (i * i <= n)
    {
        while (n % i == 0)
        {
            vec.push_back(i);
            n /= i;
            i++;
        }
    }
    if (n != 1)
    {
        vec.push_back(i);
    }
    for (int i = 0; i < vec.size(); i++)
    {
        cout << vec[i] << " ";
    }
    
    return 0;
}