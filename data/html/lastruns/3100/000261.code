#include <iostream>

using namespace std;

int main()
{
    int n;
    int bull = 0;
    std::cin >> n;
    for (int i=2; i*i<n; i++)
        {
        if (n % i == 0)
            {
            bull = 1;
            }
        }
        if (bull == 1)
        {
        std::cout << "composite";
        }
        else
        {
        std::cout << "prime";
        }
    return 0;
}