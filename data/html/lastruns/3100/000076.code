#include <iostream>

using namespace std;

int main()
{
    long long int n;
    cin >> n;
    for(int i = 2; i * i <= n; i++)
    {
        if(!(n % i))
        {
            cout << "composite";
            return 0;
        }
    }
    cout << "prime";
    return 0;
}
