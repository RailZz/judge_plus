#include <iostream>

using namespace std;

int main()
{
    int n,k=0;
    cin>>n;
    int i=2;
    while (i*i<n)
    {
        if (n%i==0) k++;
        i++;
    }
    if (k>0) cout<<"composite";
    else cout<<"prime";
    return 0;
}