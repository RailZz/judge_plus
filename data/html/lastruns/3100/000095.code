#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n;
    cin >> n;
    long long fact = 1;
    for (int i = 0; i + 2 < n; ++i) fact *= (i + 2);
    cout << fact + 2;

    return 0;
}