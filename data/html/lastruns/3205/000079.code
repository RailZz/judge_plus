#include <iostream>
#include <cstdio>
#include <vector>
using namespace std;
int main(){
	int h;
	vector<int> a;
	while(scanf( "%d ",&h) == 1){
		a.push_back(h);
	}
	for( int i = 0; i < a.size(); ++i){
		cerr << a[i] << ' ';
	} 
	for( int i = 0; i < a.size(); i = i + 2){
		cout << a[i] << ' '; 
	}
}